# Развертывание проекта BuhWeb

## Подготовка к развертыванию

Проект готов к развертыванию на различных платформах. Все необходимые конфигурационные файлы уже созданы.

## 🚀 Развертывание на Coolify

### Требования

- Репозиторий на GitHub/GitLab
- Установленный Coolify

### Шаги

1. **Загрузите код в репозиторий:**

   ```bash
   git add .
   git commit -m "Deploy to Coolify"
   git push origin main
   ```

2. **Настройте Coolify:**
   - Откройте панель Coolify
   - Создайте новое приложение
   - Выберите ваш репозиторий
   - Используйте预设 `coolfy.yaml`

3. **Конфигурация в Coolify:**
   - План: Free/Pro
   - Регион: Frankfurt (fra)
   - Node.js: 18
   - Build Command: `npm run build`
   - Start Command: `npm run preview`

## 🌐 Другие платформы

### Vercel

- Файл конфигурации: `vercel.json`
- Автоматическое развертывание при push в main
- Поддержка SPA маршрутизации

### Netlify

- Файл конфигурации: `netlify.toml`
- Непрерывное развертывание
- Автоматические редиректы для SPA

### GitHub Pages

```bash
# Добавьте в package.json:
"homepage": "https://username.github.io/buhweb",
"scripts": {
  "predeploy": "npm run build",
  "deploy": "gh-pages -d dist"
}
```

## 📦 Сборка проекта

```bash
# Локальная сборка
npm run build

# Предпросмотр сборки
npm run preview
```

## 🔧 Переменные окружения

Обязательно установите в панели хостинга:

- `NODE_ENV=production`
- `VITE_API_URL=https://your-api-domain.com`

## 📁 Структура файлов развертывания

```
buhweb/
├── dist/                 # Сборка проекта
├── coolfy.yaml           # Конфиг Coolify
├── vercel.json           # Конфиг Vercel
├── netlify.toml          # Конфиг Netlify
├── .dockerignore          # Исключения для Docker
└── README_DEPLOY.md       # Этот файл
```

## ⚡ Оптимизация

- PWA готов к работе
- Кэширование статических ресурсов
- Оптимизированные бандлы
- Source maps для отладки
- Ленивая загрузка компонентов

## 🛠️ Отладка

```bash
# Проверка сборки
npm run build -- --analyze

# Локальное тестирование
npm run preview
```

## 📊 Мониторинг

После развертывания:

- Проверьте консоль браузера
- Протестируйте PWA функционал
- Проверьте админ-панель
- Проверьте мобильную версию

## 🔄 CI/CD

Автоматическое развертывание настроено для:

- GitHub Actions (если нужно)
- Вебхуки (Coolify, Vercel, Netlify)

## 📝 Примечания

1. **API URL:** Замените `https://your-api-domain.com` на реальный адрес API
2. **HTTPS:** Убедитесь что все ресурсы загружаются по HTTPS
3. **PWA:** Манифест и service worker работают автоматически
4. **SEO:** Мета-теги и sitemap генерируются автоматически

## 🆘 Поддержка

При проблемах с развертыванием:

1. Проверьте логи сборки
2. Убедитесь что все зависимости установлены
3. Проверьте переменные окружения
4. Очистите кэш браузера
